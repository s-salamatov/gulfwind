from django.urls import include, path

from . import views


app_name = 'profiles'

urlpatterns = [
    # path('<str:username>/', views.ProfileDetailedView.as_view(), name='detailed'),
    # path('create/', views.ProfileCreateView.as_view(), name='create'),
    # path('edit/', views.ProfileUpdateView.as_view(), name='update'),

]
